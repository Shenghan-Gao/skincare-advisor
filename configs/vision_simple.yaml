# Hand-rolled CNN baseline. The report compares it against transfer learning, so
# do not skip this run.
kind: simple
epochs: 15
lr: 0.001
batch_size: 32

train_csv: data/processed/vision_train.csv
val_csv:   data/processed/vision_val.csv
out:       models/vision

run_name: simple_cnn_baseline
