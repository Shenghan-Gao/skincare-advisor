# ---- copy to .env and fill in ----
OPENAI_API_KEY=sk-...
HF_TOKEN=hf_...

# Set to 1 to run the API with mock responses (no models needed).
# Teammates working on UI / deploy / safety keep this at 1.
USE_MOCKS=1

VISION_CKPT=models/vision/best.pt
LLM_BASE_MODEL=Qwen/Qwen2.5-1.5B-Instruct
LLM_ADAPTER=models/llm/sft-lora
VECTOR_INDEX=data/processed/index
